const plugin = {
    onMessage: (client, message) => {
    },
    onConnect: (client) => {
    },
    onDisconnect: (client) => {
    },
    onEvent: (event, args) => {
    },
    onLoad: (server) => {
    }
}

module.exports = plugin